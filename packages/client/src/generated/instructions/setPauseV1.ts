/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type SetPauseV1InstructionAccounts = {
  /** Fusion data account. */
  fusionData?: PublicKey | Pda;
  /** Current authority */
  authority?: Signer;
};

// Data.
export type SetPauseV1InstructionData = {
  discriminator: Uint8Array;
  paused: boolean;
};

export type SetPauseV1InstructionDataArgs = { paused: boolean };

export function getSetPauseV1InstructionDataSerializer(): Serializer<
  SetPauseV1InstructionDataArgs,
  SetPauseV1InstructionData
> {
  return mapSerializer<
    SetPauseV1InstructionDataArgs,
    any,
    SetPauseV1InstructionData
  >(
    struct<SetPauseV1InstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['paused', bool()],
      ],
      { description: 'SetPauseV1InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([72, 174, 209, 198, 115, 3, 4, 128]),
    })
  ) as Serializer<SetPauseV1InstructionDataArgs, SetPauseV1InstructionData>;
}

// Args.
export type SetPauseV1InstructionArgs = SetPauseV1InstructionDataArgs;

// Instruction.
export function setPauseV1(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: SetPauseV1InstructionAccounts & SetPauseV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'tokenFusion',
    '5KH8Y5eFJDjhqdQki5BEiomaCBEmkmAMALS6iBS4P4EV'
  );

  // Accounts.
  const resolvedAccounts = {
    fusionData: {
      index: 0,
      isWritable: true as boolean,
      value: input.fusionData ?? null,
    },
    authority: {
      index: 1,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetPauseV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.fusionData.value) {
    resolvedAccounts.fusionData.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([102, 117, 115, 105, 111, 110, 95, 100, 97, 116, 97])
      ),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetPauseV1InstructionDataSerializer().serialize(
    resolvedArgs as SetPauseV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
