/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DestroyV1InstructionAccounts = {
  /** Fusion data account. */
  fusionData?: PublicKey | Pda;
  /**
   * Authority PDA account.
   *
   */

  authorityPda?: PublicKey | Pda;
  /** Authority and payer of the transaction. */
  authority?: Signer;
  /** Mint account of the token. */
  tokenMint: PublicKey | Pda;
  /** Token escrow pda ata account. */
  escrowAtaPda?: PublicKey | Pda;
  /** Authority ata. */
  authorityAta?: PublicKey | Pda;
  /**
   * Collection account
   *
   */

  collection: PublicKey | Pda;
  /** SPL Token program. */
  tokenProgram?: PublicKey | Pda;
  /** SPL Associated Token program. */
  associatedTokenProgram?: PublicKey | Pda;
  /** MPL Core program. */
  coreProgram?: PublicKey | Pda;
  /** System program. */
  systemProgram?: PublicKey | Pda;
  /** The SPL Noop program. */
  logWrapper?: PublicKey | Pda;
};

// Data.
export type DestroyV1InstructionData = { discriminator: Uint8Array };

export type DestroyV1InstructionDataArgs = {};

export function getDestroyV1InstructionDataSerializer(): Serializer<
  DestroyV1InstructionDataArgs,
  DestroyV1InstructionData
> {
  return mapSerializer<
    DestroyV1InstructionDataArgs,
    any,
    DestroyV1InstructionData
  >(
    struct<DestroyV1InstructionData>([['discriminator', bytes({ size: 8 })]], {
      description: 'DestroyV1InstructionData',
    }),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([239, 99, 81, 125, 255, 222, 125, 92]),
    })
  ) as Serializer<DestroyV1InstructionDataArgs, DestroyV1InstructionData>;
}

// Instruction.
export function destroyV1(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: DestroyV1InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'tokenFusion',
    '5KH8Y5eFJDjhqdQki5BEiomaCBEmkmAMALS6iBS4P4EV'
  );

  // Accounts.
  const resolvedAccounts = {
    fusionData: {
      index: 0,
      isWritable: true as boolean,
      value: input.fusionData ?? null,
    },
    authorityPda: {
      index: 1,
      isWritable: false as boolean,
      value: input.authorityPda ?? null,
    },
    authority: {
      index: 2,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
    tokenMint: {
      index: 3,
      isWritable: false as boolean,
      value: input.tokenMint ?? null,
    },
    escrowAtaPda: {
      index: 4,
      isWritable: true as boolean,
      value: input.escrowAtaPda ?? null,
    },
    authorityAta: {
      index: 5,
      isWritable: true as boolean,
      value: input.authorityAta ?? null,
    },
    collection: {
      index: 6,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    associatedTokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    coreProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.coreProgram ?? null,
    },
    systemProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    logWrapper: {
      index: 11,
      isWritable: false as boolean,
      value: input.logWrapper ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.fusionData.value) {
    resolvedAccounts.fusionData.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([102, 117, 115, 105, 111, 110, 95, 100, 97, 116, 97])
      ),
    ]);
  }
  if (!resolvedAccounts.authorityPda.value) {
    resolvedAccounts.authorityPda.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 117, 116, 104, 111, 114, 105, 116, 121])
      ),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'tokenProgram',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.escrowAtaPda.value) {
    resolvedAccounts.escrowAtaPda.value = context.eddsa.findPda(
      context.programs.getPublicKey(
        'associatedTokenProgram',
        'associatedTokenProgram'
      ),
      [
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.authorityPda.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.tokenProgram.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.tokenMint.value)
        ),
      ]
    );
  }
  if (!resolvedAccounts.authorityAta.value) {
    resolvedAccounts.authorityAta.value = context.eddsa.findPda(
      context.programs.getPublicKey(
        'associatedTokenProgram',
        'associatedTokenProgram'
      ),
      [
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.authority.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.tokenProgram.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.tokenMint.value)
        ),
      ]
    );
  }
  if (!resolvedAccounts.associatedTokenProgram.value) {
    resolvedAccounts.associatedTokenProgram.value =
      context.programs.getPublicKey(
        'associatedTokenProgram',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      );
    resolvedAccounts.associatedTokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.coreProgram.value) {
    resolvedAccounts.coreProgram.value = context.programs.getPublicKey(
      'coreProgram',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.coreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDestroyV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
