/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { findAssociatedTokenPda } from '@metaplex-foundation/mpl-toolbox';
import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import {
  AssetDataV1,
  AssetDataV1Args,
  FeeDataV1,
  FeeDataV1Args,
  getAssetDataV1Serializer,
  getFeeDataV1Serializer,
} from '../types';

// Accounts.
export type InitV1InstructionAccounts = {
  /** Fusion data account. */
  fusionData?: PublicKey | Pda;
  /**
   * Authority PDA used to verify minted NFTs to the collection
   * and to authorize transfers from token escrow.
   *
   */

  authorityPda?: PublicKey | Pda;
  /**
   * Authority. This is the address that controls the upate of the program.
   * And is also the authority of the collection.
   * Must be a signer.
   */

  authority?: Signer;
  /** Payer of the transaction. */
  payer?: Signer;
  /** Mint account of the token. */
  tokenMint: PublicKey | Pda;
  /** Token escrow pda ata account. */
  escrowAtaPda?: PublicKey | Pda;
  /** Collection account */
  collection: PublicKey | Pda;
  /** Token program. */
  tokenProgram?: PublicKey | Pda;
  /** Associated Token program. */
  associatedTokenProgram?: PublicKey | Pda;
  /** MPL Core program. */
  coreProgram?: PublicKey | Pda;
  /** System program. */
  systemProgram?: PublicKey | Pda;
  /** The SPL Noop program. */
  logWrapper?: PublicKey | Pda;
};

// Data.
export type InitV1InstructionData = {
  discriminator: Uint8Array;
  assetData: AssetDataV1;
  feeData: FeeDataV1;
};

export type InitV1InstructionDataArgs = {
  assetData: AssetDataV1Args;
  feeData: FeeDataV1Args;
};

export function getInitV1InstructionDataSerializer(): Serializer<
  InitV1InstructionDataArgs,
  InitV1InstructionData
> {
  return mapSerializer<InitV1InstructionDataArgs, any, InitV1InstructionData>(
    struct<InitV1InstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['assetData', getAssetDataV1Serializer()],
        ['feeData', getFeeDataV1Serializer()],
      ],
      { description: 'InitV1InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([152, 240, 247, 186, 91, 13, 124, 136]),
    })
  ) as Serializer<InitV1InstructionDataArgs, InitV1InstructionData>;
}

// Args.
export type InitV1InstructionArgs = InitV1InstructionDataArgs;

// Instruction.
export function initV1(
  context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs'>,
  input: InitV1InstructionAccounts & InitV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'tokenFusion',
    'STFyNpLRuUnxko7TPNqNR1g1EapVj4AnXkAFy2TGbj3'
  );

  // Accounts.
  const resolvedAccounts = {
    fusionData: {
      index: 0,
      isWritable: true as boolean,
      value: input.fusionData ?? null,
    },
    authorityPda: {
      index: 1,
      isWritable: true as boolean,
      value: input.authorityPda ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    payer: {
      index: 3,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    tokenMint: {
      index: 4,
      isWritable: false as boolean,
      value: input.tokenMint ?? null,
    },
    escrowAtaPda: {
      index: 5,
      isWritable: true as boolean,
      value: input.escrowAtaPda ?? null,
    },
    collection: {
      index: 6,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    associatedTokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    coreProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.coreProgram ?? null,
    },
    systemProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    logWrapper: {
      index: 11,
      isWritable: false as boolean,
      value: input.logWrapper ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.fusionData.value) {
    resolvedAccounts.fusionData.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([102, 117, 115, 105, 111, 110, 95, 100, 97, 116, 97])
      ),
    ]);
  }
  if (!resolvedAccounts.authorityPda.value) {
    resolvedAccounts.authorityPda.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 117, 116, 104, 111, 114, 105, 116, 121])
      ),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.escrowAtaPda.value) {
    resolvedAccounts.escrowAtaPda.value = findAssociatedTokenPda(context, {
      mint: expectPublicKey(resolvedAccounts.tokenMint.value),
      owner: expectPublicKey(resolvedAccounts.authorityPda.value),
    });
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'tokenProgram',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.associatedTokenProgram.value) {
    resolvedAccounts.associatedTokenProgram.value =
      context.programs.getPublicKey(
        'associatedTokenProgram',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      );
    resolvedAccounts.associatedTokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.coreProgram.value) {
    resolvedAccounts.coreProgram.value = context.programs.getPublicKey(
      'coreProgram',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.coreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitV1InstructionDataSerializer().serialize(
    resolvedArgs as InitV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
