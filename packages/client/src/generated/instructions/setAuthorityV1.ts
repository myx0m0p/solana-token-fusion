/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type SetAuthorityV1InstructionAccounts = {
  /** Fusion data account. */
  fusionData?: PublicKey | Pda;
  /** Current authority */
  authority?: Signer;
};

// Data.
export type SetAuthorityV1InstructionData = {
  discriminator: Uint8Array;
  newAuthority: PublicKey;
};

export type SetAuthorityV1InstructionDataArgs = { newAuthority: PublicKey };

export function getSetAuthorityV1InstructionDataSerializer(): Serializer<
  SetAuthorityV1InstructionDataArgs,
  SetAuthorityV1InstructionData
> {
  return mapSerializer<
    SetAuthorityV1InstructionDataArgs,
    any,
    SetAuthorityV1InstructionData
  >(
    struct<SetAuthorityV1InstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['newAuthority', publicKeySerializer()],
      ],
      { description: 'SetAuthorityV1InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([108, 174, 157, 222, 103, 150, 173, 66]),
    })
  ) as Serializer<
    SetAuthorityV1InstructionDataArgs,
    SetAuthorityV1InstructionData
  >;
}

// Args.
export type SetAuthorityV1InstructionArgs = SetAuthorityV1InstructionDataArgs;

// Instruction.
export function setAuthorityV1(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: SetAuthorityV1InstructionAccounts & SetAuthorityV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'tokenFusion',
    'STFyNpLRuUnxko7TPNqNR1g1EapVj4AnXkAFy2TGbj3'
  );

  // Accounts.
  const resolvedAccounts = {
    fusionData: {
      index: 0,
      isWritable: true as boolean,
      value: input.fusionData ?? null,
    },
    authority: {
      index: 1,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetAuthorityV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.fusionData.value) {
    resolvedAccounts.fusionData.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([102, 117, 115, 105, 111, 110, 95, 100, 97, 116, 97])
      ),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetAuthorityV1InstructionDataSerializer().serialize(
    resolvedArgs as SetAuthorityV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
