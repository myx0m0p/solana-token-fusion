/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RedelegateV1InstructionAccounts = {
  /** Fusion data account. */
  fusionData?: PublicKey | Pda;
  /** Authority PDA account. */
  authorityPda?: PublicKey | Pda;
  /** Authority and payer of the transaction. */
  authority?: Signer;
  /** Collection account */
  collection: PublicKey | Pda;
  /** MPL Core program. */
  coreProgram?: PublicKey | Pda;
  /** System program. */
  systemProgram?: PublicKey | Pda;
  /** The SPL Noop program. */
  logWrapper?: PublicKey | Pda;
};

// Data.
export type RedelegateV1InstructionData = { discriminator: Uint8Array };

export type RedelegateV1InstructionDataArgs = {};

export function getRedelegateV1InstructionDataSerializer(): Serializer<
  RedelegateV1InstructionDataArgs,
  RedelegateV1InstructionData
> {
  return mapSerializer<
    RedelegateV1InstructionDataArgs,
    any,
    RedelegateV1InstructionData
  >(
    struct<RedelegateV1InstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'RedelegateV1InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([190, 200, 48, 48, 124, 158, 83, 110]),
    })
  ) as Serializer<RedelegateV1InstructionDataArgs, RedelegateV1InstructionData>;
}

// Instruction.
export function redelegateV1(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: RedelegateV1InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'tokenFusion',
    'STFyNpLRuUnxko7TPNqNR1g1EapVj4AnXkAFy2TGbj3'
  );

  // Accounts.
  const resolvedAccounts = {
    fusionData: {
      index: 0,
      isWritable: true as boolean,
      value: input.fusionData ?? null,
    },
    authorityPda: {
      index: 1,
      isWritable: false as boolean,
      value: input.authorityPda ?? null,
    },
    authority: {
      index: 2,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
    collection: {
      index: 3,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    coreProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.coreProgram ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    logWrapper: {
      index: 6,
      isWritable: false as boolean,
      value: input.logWrapper ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.fusionData.value) {
    resolvedAccounts.fusionData.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([102, 117, 115, 105, 111, 110, 95, 100, 97, 116, 97])
      ),
    ]);
  }
  if (!resolvedAccounts.authorityPda.value) {
    resolvedAccounts.authorityPda.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 117, 116, 104, 111, 114, 105, 116, 121])
      ),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.coreProgram.value) {
    resolvedAccounts.coreProgram.value = context.programs.getPublicKey(
      'coreProgram',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.coreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRedelegateV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
